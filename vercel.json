{
  "builds": [
    {
      "src": "api/data.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/data.js" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
